#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always 0
 */
int main(void)
{
	int ch;
	for ((ch = 97; ch <= 122; ch++; \n))

	{
		putchar(ch);
	}
	putchar('\n');

return (0);

}
