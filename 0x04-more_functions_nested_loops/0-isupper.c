#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: integer function
 * Return 1 if c is uppercase return 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z')
}
