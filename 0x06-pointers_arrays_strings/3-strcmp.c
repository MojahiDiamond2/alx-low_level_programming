#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: dest
 */
int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);
	return (dest);
}
